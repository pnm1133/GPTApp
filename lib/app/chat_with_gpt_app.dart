import 'package:flutter/material.dart';

class ChatWithGptApp extends StatelessWidget {
  const ChatWithGptApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
  
    return Container();
  }
}
